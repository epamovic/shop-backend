{
  "name": "shop-backend",
  "version": "0.1.0",
  "bin": {
    "shop-backend": "bin/shop-backend.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "cdk deploy",
    "build:deploy": "npm run build && npm run deploy",
    "build:deploy:product": "npm run build && cdk deploy ProductLambdaStack",
    "build:deploy:all": "npm run build && cdk deploy --all"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "@types/uuid": "^10.0.0",
    "aws-cdk": "2.1029.2",
    "esbuild": "^0.25.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.896.0",
    "aws-cdk-lib": "2.214.0",
    "aws-lambda": "^1.0.7",
    "constructs": "^10.0.0",
    "csv-parser": "^3.2.0",
    "uuid": "^10.0.0"
  }
}
